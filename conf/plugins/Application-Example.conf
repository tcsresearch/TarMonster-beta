#
# Application Variables.
# ## These are to be changed for each app TarMonster backs up.
AppName="MyAppName" 			# The Applications actual name.
AppFriendlyName="myapp" 			# The friendly name of the app.
DomainName="app.mydomain.com" 	# Domain Name
#
# AppFolder Variables.
# TODO: Create function to determine app folder name.
# AppFolder="~/app.mydomain.com"
AppFolder="./app.mydomain.com"
#
# FileName Variables.
BaseFileName="app.mydomain.com" # Do we need this to be separate from $DomainName?
BackupFileName="${BaseFileName}-${Today}"
